[project]
name = "portfolio-holding-alerts"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "kiteconnect>=5.0.1",
    "python-dotenv>=1.1.1",
    "schedule>=1.2.2",
]
